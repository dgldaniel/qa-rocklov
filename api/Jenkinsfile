pipeline {
    agent {
        docker {
            image 'ruby:2.6.5'
            args '--network rocklov-network'
        }
    }

    stages {
        stage('Prep') {
            steps {
                bat "cd api && bundle install"
            }
        }
        stage('Testing') {
            steps {
                bat "cd api && rspec"
            }
        }
    }
}
